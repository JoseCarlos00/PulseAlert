<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Permite HTTP para CUALQUIER destino (lo más simple para red local) -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>